class Solution {
public:
    string finalString(string s) {
        string ans="";
        int x=0;
        while(x<s.length())
        {
            if(s[x]=='i') reverse(ans.begin(), ans.end());
            else ans+=s[x];
            x++;
        }
        return ans;
    }
};
